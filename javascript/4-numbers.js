const num1 = 100;
const num2 = 50;

let val;

// Simple math with number
val = num1 + num2; // Add
val = num1 - num2; // Sub
val = num1 * num2; // Multiply
val = num1 / num2; // division
val = num1 % num2; // reminder

// MATH OBJECT

val = Math.PI;
val = Math.round(2.4);
val = Math.ceil(2.1); // upper number
val = Math.floor(2.9); // Lower Number
val = Math.sqrt(64);
val = Math.abs(-5); // Always return a postive value
val = Math.min(-1,4,5,6,5,67,34,23,7);
val = Math.max(-1,4,5,6,5,67,34,23,7);
val = Math.random();

val = Math.floor(Math.random() * 5);


console.log(val);